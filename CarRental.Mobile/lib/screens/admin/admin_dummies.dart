import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class AdminFleetScreen extends StatelessWidget {
  const AdminFleetScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Admin Fleet coming soon'));
}

class AdminBookingsScreen extends StatelessWidget {
  const AdminBookingsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Admin Bookings coming soon'));
}

class AdminCustomersScreen extends StatelessWidget {
  const AdminCustomersScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Admin Customers coming soon'));
}
